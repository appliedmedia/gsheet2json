{
  "name": "gsheet2json",
  "version": "1.2.40",
  "type": "module",
  "scripts": {
    "build": "bash scripts/build.sh",
    "build:gas": "bash scripts/build-gas.sh",
    "push": "npm run build && npx clasp push --force",
    "deploy": "zsh scripts/deploy.sh",
    "test": "tsc -p tsconfig.test.json && NODE_OPTIONS='--import tsx/esm' cucumber-js --tags 'not @gas and not @pre-submit'",
    "test:gas": "npm run build:gas && npx clasp push --force && NODE_OPTIONS='--import tsx/esm' cucumber-js --tags @gas",
    "test:pre-submit": "tsc -p tsconfig.test.json && NODE_OPTIONS='--import tsx/esm' cucumber-js --tags 'not @gas'",
    "test:all": "npm run test:pre-submit && npm run test:gas && npm run build",
    "record:promo": "echo 'Open Claude Code and paste: scripts/record-session.md'"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.8.2",
    "@google/clasp": "^3.2.0",
    "@types/google-apps-script": "^2.0.8",
    "@types/jsdom": "^28.0.1",
    "jsdom": "^29.1.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
